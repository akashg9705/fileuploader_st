# Streamlit File Uploader

A simple Streamlit application that allows users to upload files
and view basic file details such as name, type, and size.

## Features
- Upload files (txt, csv, pdf, images)
- Display file metadata
- Preview text files

## Run locally

```bash
pip install -r requirements.txt
streamlit run app.py
